# Vite React tiny SPA example

A standalone React counter example for `vite-plugin-tiny-spa` that people can
copy from. It emits one `dist/index.html` while loading React, React DOM, and
`sugar-high` from esm.sh.

```bash
cd examples/vite-react
npm install
npm run build
```
